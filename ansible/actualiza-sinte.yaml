---
# El sinte no sirve panel web (panel.service no está instalado en el device:
# corre solo el player en kiosk). rsync de los ficheros versionados + arranque
# del player.
- name: Actualizar sintetizador
  hosts: sinte
  gather_facts: no
  roles:
    - role: sintetizador-actualiza
